{
    "name": "foodswipe-root",
    "version": "1.0.0",
    "private": true,
  "scripts": {
    "start": "node backend/server.js",
    "install": "npm install --prefix backend",
    "build": "echo 'Skipping frontend build on backend service'"
  }
}
